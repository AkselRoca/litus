import 'server-only'
import type { ContactConfig } from '@/lib/contact/config'
import type { BriefDocument } from './model'
import { documentMarkdown } from './model'

export const BRIEF_RECIPIENT = 'litusagency@gmail.com'
export type BriefTechnical = { ip: string; receivedAt: string; userAgent: string; pageUrl: string; referrer: string; country: string; city: string; protections: string }
export type BriefEnvelope = { document: BriefDocument; technical: BriefTechnical }
const escape = (v: string) => v.replace(/[&<>"']/g, c => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' })[c]!)
const textHtml = (v: string) => escape(v).replace(/\n/g, '<br>')

function layout(envelope: BriefEnvelope, admin: boolean) {
  const doc = envelope.document
  const rows = doc.sections.map(section => `<tr><td style="padding:25px 0;border-top:1px solid #e7e8eb"><h2 style="margin:0 0 17px;font-size:20px;line-height:1.35;letter-spacing:-.4px;color:#132b43">${escape(section.title)}</h2>${section.rows.map(row => `<p style="margin:0 0 15px"><span style="display:block;font-size:12px;color:#6b7889;margin-bottom:5px">${escape(row.label)}</span><span style="font-size:14px;line-height:1.75;color:#23364c">${textHtml(row.value)}</span></p>`).join('')}${section.bullets.length ? `<ul style="padding-left:20px;margin:0;color:#3d4e60;font-size:14px;line-height:1.8">${section.bullets.map(b => `<li style="margin-bottom:9px">${escape(b)}</li>`).join('')}</ul>` : ''}</td></tr>`).join('')
  const tech = Object.entries({ 'Adresse IP (source réseau de confiance)': envelope.technical.ip, 'Date et heure de réception (UTC)': envelope.technical.receivedAt, 'User-Agent / navigateur / appareil déclaré': envelope.technical.userAgent, 'Page d’origine / URL du formulaire': envelope.technical.pageUrl, 'Referrer': envelope.technical.referrer, 'Pays approximatif': envelope.technical.country, 'Ville approximative': envelope.technical.city, 'Protections anti-spam': envelope.technical.protections }).map(([label, value]) => `<p style="margin:0 0 9px;font-size:11px;line-height:1.6;overflow-wrap:anywhere"><strong>${escape(label)}</strong><br>${textHtml(value || 'Non disponible')}</p>`).join('')
  return `<!doctype html><html lang="fr"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${admin ? 'Nouveau projet à étudier' : 'Votre cahier des charges Litus'}</title></head><body style="margin:0;background:#f6f4ef;color:#132b43;font-family:Arial,Helvetica,sans-serif"><div style="display:none;max-height:0;overflow:hidden">${admin ? 'Une nouvelle demande qualifiée et son cahier des charges.' : 'Votre projet est posé. Voici votre document complet, préparé à partir de vos réponses.'}</div><table role="presentation" width="100%" cellspacing="0" cellpadding="0" style="background:#f6f4ef"><tr><td align="center" style="padding:30px 12px"><table role="presentation" width="640" cellspacing="0" cellpadding="0" style="width:100%;max-width:640px;background:#fff;border:1px solid #e7e5df;border-radius:20px"><tr><td style="padding:32px 28px 22px"><a href="https://www.litus.fr"><img src="https://www.litus.fr/logo-sans-fond.png" width="132" alt="Litus — Stratégie digitale et acquisition" style="display:block;width:132px;max-width:100%;height:auto;border:0"></a><p style="margin:27px 0 12px;color:#dd4b1b;font-size:10px;font-weight:bold;letter-spacing:2px">${admin ? 'UN NOUVEAU PROJET À CADRER' : 'VOTRE PROJET PREND FORME'}</p><h1 style="font-family:Helvetica,Arial,sans-serif;font-size:30px;line-height:1.2;letter-spacing:-1px;margin:0 0 18px;color:#132b43">${admin ? 'Un cahier des charges à étudier.' : 'Les bonnes bases pour la suite.'}</h1><p style="font-size:15px;line-height:1.8;color:#617084;margin:0">${admin ? `${escape(doc.contact.fullName)} a préparé son projet avec le générateur Litus. Voici ses réponses et le document de cadrage transmis au prospect.` : `Bonjour ${escape(doc.contact.fullName)},<br>Voici votre cahier des charges, construit à partir de vos réponses. Vous pouvez le conserver, le partager et vous en servir pour préciser votre projet avec nous.`}</p></td></tr><tr><td style="padding:0 28px"><table role="presentation" width="100%" cellspacing="0" cellpadding="0"><tr><td style="padding:20px;background:#fbf8f3;border:1px solid #eee7de;border-radius:12px"><p style="font-size:10px;color:#cc471e;letter-spacing:1px;margin:0 0 9px">${escape(doc.reference)} · ${escape(new Date(doc.createdAt).toLocaleDateString('fr-FR', { timeZone: 'Europe/Paris' }))}</p><h2 style="font-size:21px;line-height:1.4;margin:0 0 12px;color:#132b43">${escape(doc.title)}</h2><p style="margin:0;font-size:13px;line-height:1.8;color:#526276">${escape(doc.contact.fullName)}${doc.contact.company ? ` · ${escape(doc.contact.company)}` : ''}<br><a style="color:#132b43" href="mailto:${escape(doc.contact.email)}">${escape(doc.contact.email)}</a>${doc.contact.phone ? `<br>${escape(doc.contact.phone)}` : ''}</p></td></tr></table></td></tr><tr><td style="padding:25px 28px 0"><table role="presentation" width="100%" cellspacing="0" cellpadding="0">${rows}</table></td></tr><tr><td style="padding:5px 28px 30px"><h2 style="font-size:21px;color:#132b43;margin:0 0 12px">Et maintenant ?</h2><p style="font-size:14px;line-height:1.8;color:#617084;margin:0 0 20px">${admin ? 'Reprendre les objectifs et les points à préciser avant de proposer un périmètre et un devis adaptés. Le document ci-dessous peut être copié dans votre outil de travail ; retirer les coordonnées avant tout partage avec un service tiers.' : 'Arthur ou Aksel va étudier votre demande et vous recontactera rapidement pour échanger sur votre projet et vous proposer la solution la plus adaptée.'}</p><table role="presentation" cellspacing="0" cellpadding="0"><tr><td style="background:#fa541c;border-radius:999px;text-align:center"><a href="${admin ? `mailto:${escape(doc.contact.email)}` : 'tel:+33744985521'}" style="display:block;padding:16px 22px;color:#fff;font-size:13px;font-weight:bold;text-decoration:none;line-height:1.5">${admin ? 'Répondre au prospect' : 'Une précision à ajouter ? Appelez-nous au 07 44 98 55 21'}</a></td></tr></table><p style="margin:22px 0 0;font-size:11px;line-height:1.7;color:#7b8694">Ce document constitue une base de discussion, pas un devis ni un engagement contractuel. Le périmètre, le prix et les responsabilités restent à valider ensemble.</p>${admin ? `<details style="margin-top:25px"><summary style="font-weight:bold;font-size:13px">Version texte à copier dans votre outil de travail</summary><pre style="white-space:pre-wrap;overflow-wrap:anywhere;font:12px/1.7 monospace;color:#445469">${escape(documentMarkdown({ ...doc, contact: { fullName: '[Contact à compléter]', email: '[Email retiré]', company: doc.contact.company } }))}</pre></details><div style="margin-top:28px;padding-top:20px;border-top:1px solid #e7e8eb;color:#6b7889"><h2 style="font-size:15px;color:#132b43;margin:0 0 14px">Informations techniques</h2>${tech}<p style="font-size:10px;line-height:1.6">Indicateurs de sécurité, pas une preuve d’identité. Les données déclarées et la localisation IP peuvent être inexactes. Ne pas transmettre ces informations à un outil tiers sans nécessité.</p></div>` : ''}</td></tr><tr><td style="padding:23px 28px;border-top:1px solid #eceae6;background:#fcfbf8;border-radius:0 0 20px 20px"><p style="margin:0;font-size:13px;font-weight:bold;color:#132b43">Litus · Stratégie digitale &amp; acquisition</p><p style="margin:6px 0 0;font-size:12px;line-height:1.8;color:#718092">Lorient · Le Mans<br><a href="https://www.litus.fr" style="color:#d94b1d;text-decoration:none">litus.fr</a> · <a href="https://www.litus.fr/politique-confidentialite" style="color:#718092">Confidentialité</a></p></td></tr></table></td></tr></table></body></html>`
}
export async function sendBriefEmails(config: ContactConfig, envelope: BriefEnvelope, submissionId: string) {
  for (const admin of [true, false]) {
    const doc = envelope.document
    const text = documentMarkdown(doc) + (admin ? `\n\nInformations techniques\n${Object.entries(envelope.technical).map(([key, value]) => `${key}: ${value}`).join('\n')}` : '\n\nArthur ou Aksel reviendra vers vous pour échanger sur votre projet.')
    const response = await fetch('https://api.resend.com/emails', {
      method: 'POST', cache: 'no-store', signal: AbortSignal.timeout(12000),
      headers: { Authorization: `Bearer ${config.RESEND_API_KEY}`, 'Content-Type': 'application/json', 'Idempotency-Key': `brief/${submissionId}/${admin ? 'admin' : 'client'}` },
      body: JSON.stringify({ from: `Litus <${config.RESEND_FROM_EMAIL}>`, to: [admin ? BRIEF_RECIPIENT : doc.contact.email], reply_to: admin ? doc.contact.email : BRIEF_RECIPIENT, subject: admin ? `Nouveau cahier des charges · ${doc.reference}` : `Votre cahier des charges Litus · ${doc.reference}`, html: layout(envelope, admin), text }),
    })
    const result = await response.json().catch(() => null)
    if (!response.ok || !result?.id) throw new Error(`Brief email provider failure (${response.status})`)
  }
}
